import type {
  AcceptConfig,
  RcFile,
  UploadProps,
  UploadRequestOption,
} from './interface';
import Upload from './Upload';

export type {
  AcceptConfig,
  RcFile,
  UploadProps,
  UploadRequestOption,
};

export default Upload;
